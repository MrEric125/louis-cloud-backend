

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/



server:
  port: 8888

#  基于git的配置
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/MrEric125/springcloud
          search-paths: config
          username:
          password:

